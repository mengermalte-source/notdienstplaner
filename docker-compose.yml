services:
  app:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - db-data:/data
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite+aiosqlite:////data/notdienstplaner.db
    restart: unless-stopped

volumes:
  db-data:
